# ===============================================
#       BIẾN MÔI TRƯỜNG CHO ỨNG DỤNG
# ===============================================

# --- Cấu hình Server ---
# Cổng mà server Node.js sẽ lắng nghe. 5000 là một lựa chọn phổ biến cho môi trường phát triển.
PORT=5000

# --- Cấu hình Cơ sở dữ liệu (MongoDB Atlas) ---
# Dán chuỗi kết nối của bạn vào đây.
# QUAN TRỌNG: Thay thế <your_new_password> bằng mật khẩu MỚI bạn đã tạo trên MongoDB Atlas.
# Giữ nguyên tên người dùng (pxmanhctc) và tên cơ sở dữ liệu (Solar) nếu bạn không thay đổi chúng.


MONGO_URI=mongodb+srv://pxmanhctc:ucJNc2gtKAzztdc6@cluster0.234t0ws.mongodb.net/solaranalytics?retryWrites=true&w=majority&appName=Cluster0
#MONGO_URI=mongodb://localhost:27017/solaranalytics
#MONGO_URI=mongodb://221.132.29.150:27017/solaranalytics

# --- Cấu hình CORS (Cross-Origin Resource Sharing) ---
# URL của ứng dụng client (frontend).
# Điều này cho phép trang web ở địa chỉ này gọi API từ server của bạn một cách an toàn.
# Nếu bạn dùng extension "Live Server" của VS Code, cổng mặc định thường là 5500.
CLIENT_URL=http://127.0.0.1:5500

# --- Cấu hình Email (Gmail SMTP) ---
# Email Gmail để gửi email xác thực
# QUAN TRỌNG: Thay thế bằng email Gmail thật của bạn
EMAIL_USER=pxmanhctc@gmail.com

# App Password của Gmail (không phải mật khẩu thường)
# Hướng dẫn tạo App Password:
# 1. Vào Google Account Settings
# 2. Security > 2-Step Verification
# 3. App passwords > Generate app password
# 4. Chọn "Mail" và thiết bị của bạn
# 5. Copy mật khẩu 16 ký tự và dán vào đây
EMAIL_PASS=wrutbkloiaiejyyx

# --- Cấu hình JSON Web Token (JWT) ---
# Chuỗi bí mật này dùng để ký và xác thực token. Hãy giữ nó an toàn.
# Bạn có thể tạo một chuỗi ngẫu nhiên phức tạp ở đây.
JWT_SECRET=mot_chuoi_bi_mat_rat_dai_va_kho_doan_123456
